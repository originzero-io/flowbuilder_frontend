const name = "notes";

export const GET_NOTES = `${name}/getNotes`;
export const CREATE_NOTE = `${name}/createNote`;
export const DELETE_NOTE = `${name}/deleteNote`;
export const UPDATE_NOTE = `${name}/updateNote`;